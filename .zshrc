ETC_ZSHRC=/etc/zshrc
[ -r ${ETC_ZSHRC} ] && . ${ETC_ZSHRC}

####################################################################################################

export ALIAS="${HOME}/.alias"
export ENVRC="${HOME}/.envrc"

####################################################################################################

[ -r ${ENVRC} ] && . ${ENVRC}
[ -r ${ALIAS} ] && . ${ALIAS}
